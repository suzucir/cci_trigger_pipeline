version: 2.1

parameters:
  action:
    type: enum
    enum: [build, deploy]
    default: build

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Build"
          run: echo "ビルド用のジョブです！"
  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Deployment"
          run: echo "デプロイ用のジョブです！"

workflows:
  build-workflow:
    when:
      equal: [ build, << pipeline.parameters.action >> ]
    jobs:
      - build

  deploy-workflow:
    when:
      equal: [ build, << pipeline.parameters.action >> ]
    jobs:
      - deploy
          filters:
            branches:
              only:
                - main
